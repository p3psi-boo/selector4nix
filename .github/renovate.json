{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "ignorePaths": [
    "crates/selector4nix-workspace-hack/Cargo.toml"
  ],
  "packageRules": [
    {
      "description": "Rust dependencies",
      "matchManagers": ["cargo"],
      "commitMessagePrefix": "chore(deps): ",
      "commitMessageAction": "bump",
      "commitMessageTopic": "Rust dependency {{depName}}",
      "commitMessageExtra": "to {{newVersion}}"
    },
    {
      "description": "Node.js dependencies",
      "matchManagers": ["npm"],
      "commitMessagePrefix": "chore(deps): ",
      "commitMessageAction": "bump",
      "commitMessageTopic": "Node.js dependency {{depName}}",
      "commitMessageExtra": "to {{newVersion}}"
    },
    {
      "description": "Nix Flake inputs",
      "matchManagers": ["nix"],
      "commitMessagePrefix": "chore(deps): ",
      "commitMessageAction": "bump",
      "commitMessageTopic": "Nix Flake input {{depName}}",
      "commitMessageExtra": "to {{newDigest}}"
    },
    {
      "description": "Stop updating nixpkgs-nix-serve-ng digest in flake.nix",
      "matchDepNames": ["nixpkgs-nix-serve-ng"],
      "enabled": false
    }
  ],
  "nix": {
    "enabled": true
  },
  "rust-toolchain": {
    "enabled": false
  }
}
